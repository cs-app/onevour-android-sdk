package org.cise.sdk.ciseapp.modules.chat;

import org.cise.core.utilities.ui.adapter.recyclerview.AdapterModel;

public class MessageModel extends AdapterModel<ChatMessage> {

    public MessageModel(ChatMessage model) {
        super(model);
    }
}
